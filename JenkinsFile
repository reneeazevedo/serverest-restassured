pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                bat 'mvn clean test'
            }
        }
        stage('Allure Report') {
            steps {
                bat 'allure generate target/surefire-reports --clean -o target/allure-report'
                allure includeProperties: false, jdk: '', properties: [], reportBuildPolicy: 'ALWAYS', results: [[path: 'target/allure-report']]
        }
    }
  }
}