pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                bat 'mvn clean'
            }
        }
        stage('Allure Report') {
            steps {
                bat 'mvn allure:serve target/allure-report'
                allure includeProperties: false, jdk: '', properties: [], reportBuildPolicy: 'ALWAYS', results: [[path: 'target/allure-report']]
        }
    }
  }
}